# ===========================================
# AnswerMe 环境变量配置示例
# 复制此文件为 .env 并修改相应值
# ===========================================

# -----------------------
# 数据库配置
# -----------------------
POSTGRES_DB=answermedb
POSTGRES_USER=answeruser
# ⚠️  安全警告: 请在生产环境部署前更改此密码
# 生成强密码命令: openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_THIS_BEFORE_DEPLOYMENT_generate_with_openssl_rand_base64_32
POSTGRES_PORT=5432

# -----------------------
# JWT认证配置
# -----------------------
# ⚠️  安全警告: 请在生产环境部署前更改此密钥(至少32字符)
# 生成密钥命令: openssl rand -base64 64
JWT_SECRET=CHANGE_THIS_BEFORE_DEPLOYMENT_generate_with_openssl_rand_base64_64_minimum_32_chars
JWT_EXPIRY_DAYS=30
JWT_ISSUER=AnswerMe
JWT_AUDIENCE=AnswerMeUsers

# -----------------------
# 应用配置
# -----------------------
ASPNETCORE_ENVIRONMENT=Development
BACKEND_PORT=5000

# CORS配置(允许的前端来源,逗号分隔)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# 可选: Production, Staging
# FRONTEND_PORT=3000
# VITE_API_BASE_URL=http://localhost:5000

# -----------------------
# 日志配置
# -----------------------
# LOG_LEVEL=Information
# 可选: Debug, Information, Warning, Error

# -----------------------
# 限流配置（可选）
# -----------------------
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_PER_MINUTE=60

# -----------------------
# 备份配置（可选）
# -----------------------
# BACKUP_ENABLED=false
# BACKUP_RETENTION_DAYS=7
# BACKUP_CRON="0 2 * * *"  # 每天凌晨2点
